{
  "name": "swiss-rb-knife",
  "private": true,
  "version": "0.7.0",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "description": "A Multi-Tool Application.",
  "authors": "Railblade",
  "scripts": {
    "predev": "node scripts/copy-public-docs.mjs && node scripts/generate-licenses.mjs && node scripts/generate-sound-manifest.mjs && node scripts/build-agent.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-public-docs.mjs && node scripts/generate-licenses.mjs && node scripts/generate-sound-manifest.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:agent": "node scripts/build-agent.mjs --release",
    "tauri": "tauri",
    "test": "node --test \"scripts/checks/*.test.mjs\"",
    "test:rust": "cd src-tauri && cargo test --lib",
    "check": "npm run test && npm run test:rust"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^25.6.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
